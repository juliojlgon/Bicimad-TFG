@model PagedList.StaticPagedList<Bicimad.Services.Query.Dto.History.UserHistoryDto>
@{
    ViewBag.Title = "Zona privada";
}

<div class="container">
    @foreach (var user in Model)
    {
        @user.DepartureStationId
    }
</div>

